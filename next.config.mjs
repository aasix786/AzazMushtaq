/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    distDir:"dist",
    basePath: '/AzazMushtaq',
    images: {
      unoptimized: true,
    },
};

export default nextConfig;
